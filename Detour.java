import java.util.*;

public class Detour{
    PriorityQueue<Integer> dijkstra = new PriorityQueue<>();
    ArrayList<PriorityQueue<Integer>> adjlist = new ArrayList<>();

    public static void main(String[] args){
        // Read in the input
        try {
            BufferedReader r = new BufferedReader(new InputStreamReader(System.in));
            String[] ph = r.readLine().split(" ");
            int n = Integer.parseInt(pharr[0]);
            int m = Integer.parseInt(pharr[1]);
            for (int i=0; i<n; ++i){ 
                String[] ph = r.readLine().split(" ");
                
        }
        catch (IOException e){}
    }
}
